# DESIGN-AUDIT — מגדל הים (Sea Tower)

**תאריך:** 2026-08-14 · **מקור:** צילומי מסך חיים (320–1440px) מכל 13 העמודים הציבוריים + קריאת קוד מלאה
**צילומי לפני:** `scratchpad/audit-before/*.png` (נשמרו מחוץ לריפו)

---

## תמצית

האתר בנוי טכנית מצוין (RTL, נגישות, ביצועים סבירים, אפס גלישה אופקית מ-320px) — אבל ויזואלית הוא
מדבר בשפה של **דף נחיתה SaaS**, לא של **מותג אירוח ים-תיכוני**: גרדיאנטים כחולים, בועות ואורבים
מונפשים, כרטיסים לבנים מעוגלים בכל מקום, ותבנית עמוד אחת שחוזרת על עצמה בכל הראוטים.
הצילום האמיתי — הנכס החזק ביותר של האתר (דירות אמיתיות, נוף אמיתי, צילום אווירי של בניין אלמוג) —
כלוא בתוך תיבות קטנות.

---

## ממצא 1 — תבנית "hero נייבי + גל" זהה בכל עמוד (החמור ביותר)

- **דפוס:** `linear-gradient(120deg, navy-900, ocean-700, ocean-600)` + blobs מונפשים + פירורי לחם +
  H1 ממורכז עם שורה בגרדיאנט-טקסט + תת-כותרת + `WaveSeparator`. מועתק-מודבק (לא קומפוננטה!) ב-7 קבצים.
- **ראוטים:** `/rooms`, `/about`, `/solutions`, `/contact`, `/faq`, `/house-rules`, `/terms`, `/articles`
  (+ וריאציה מונפשת ב-`/articles/[slug]`, + ה-hero של הבית, + ה-404).
- **למה גנרי:** היררכיה ויזואלית זהה בעמודים שאין ביניהם שום קשר — תקנון משפטי נראה כמו עמוד שיווקי.
  גרדיאנט-על-כותרת הוא טביעת אצבע מובהקת של אתרי AI/SaaS.
- **החלפה:** משפחות עמודים (ראו "כיוון עיצובי" למטה) — עמודי תוכן/משפטי נפתחים בפתיח טיפוגרפי שקט על
  רקע בהיר; עמודי שיווק נפתחים בצילום; רק מקומות נבחרים נשארים כהים.
- **היקף:** shared — קומפוננטת `PageIntro` אחת עם וריאנטים + קומפוזיציה עמוד-ספציפית.

## ממצא 2 — בועות, אורבים, blobs וקונפטי

- **דפוס:** `st-bub`/`st-orb`/`stm-blob`/`ar-blob` — עשרות אלמנטים דקורטיביים מונפשים (14 בועות ב-hero
  הבית בלבד), קונפטי 70 אלמנטים באישור הזמנה.
- **ראוטים:** `/` (hero + צור קשר), כל 7 עמודי ה-hero, `/articles/[slug]` (גרדיאנט נושם + 2 blobs).
- **למה גנרי:** "blobs" נמצאים ברשימת האיסורים המפורשת של הברייף (§36). הם מסמנים "אתר AI" ואינם קשורים
  לאירוח. הים האמיתי נמצא במרחק 50 מ' — אין צורך לצייר אותו ב-CSS.
- **החלפה:** צילום אמיתי (וידאו הים הקיים, צילום אווירי, תצלומי דירות) + רקעים שקטים. הסרה מלאה של
  `home.css` bubbles/orbs.
- **היקף:** shared (מחיקת CSS) + עמוד-ספציפי.

## ממצא 3 — הכול כרטיס לבן מעוגל

- **דפוס:** `bg-white + rounded-[18-26px] + border + shadow` כפתרון ברירת מחדל לכל תוכן:
  StatCard, כרטיסי פתרונות, כרטיסי ערכים (about), כרטיסי צעדים (contact/solutions), כרטיסי מאמרים,
  FAQ accordions, טופס צור קשר, תיבת CTA גרדיאנט מעוגלת (זהה ב-3 עמודים).
- **למה גנרי:** "white background + border + shadow + radius + icon + title + paragraph" — הדפוס
  שהברייף מצטט מילה במילה (§5). שום דבר בקיבוץ הזה לא מוסיף משמעות.
- **החלפה:**
  - סטטיסטיקות → מספרים חשופים עם קווי hairline (בלי קופסאות).
  - צעדים → רשימה ממוספרת אדיטוריאלית עם ספרות גדולות, בלי קופסאות.
  - ערכים/יתרונות → שורות טקסט עם קו מפריד עדין, אייקונים מוסרים או מוקטנים.
  - CTA box → פס full-bleed כהה עם צילום או טיפוגרפיה, לא "קופסה בתוך עמוד".
  - FAQ → נשאר `<details>` (נגישות) אבל בשפה של רשימת מסמך עם קווי hairline.
- **היקף:** shared (`StatCard`, `IconTile`, `CheckItem` מעוצבים מחדש) + החלפות פר-עמוד.

## ממצא 4 — הצילום כלוא

- **דפוס:** תמונות אמיתיות מוצגות ב-`h-[200-220px]` בתוך כרטיסים, `rounded-img 22px`, תמיד באותו יחס.
- **ראוטים:** `/rooms` (גריד 3 עמודות אחיד), קרוסלת הבית, `/solutions`, `/about`, `/articles`.
- **למה בעיה:** הדירות והנוף הם המוצר. ברייף §6: "Photography should have significantly more visual authority".
- **החלפה:** hero צילומי מלא בבית; גריד אדיטוריאלי א-סימטרי ב-`/rooms` (דירה מובילה גדולה, השאר משתנות);
  תמונות full-bleed בין מקטעים; רדיוסים מוקטנים (טוקן חדש); crop אדריכלי.
- **היקף:** shared (`RoomCard`) + קומפוזיציה פר-עמוד.

## ממצא 5 — גימיקים תנועתיים גנריים

- **דפוס:** CTA מכונת-כתיבה ב-Header (מקליד/מוחק "הזמינו עכשיו"/"בדיקת זמינות"), פס התקדמות גלילה בכל
  עמוד, פיצול כותרות מילה-מילה (`data-ws`), פרלקסה, כרטיסים צפים `animate-float`, מרקיזת לוגואים,
  מונה CSS "0→50".
- **למה גנרי:** ברייף §36 — "animation on every element", "visual tricks unrelated to hospitality".
  מכונת כתיבה ב-CTA ראשי פוגעת גם בקריאות וגם באמינות.
- **החלפה:** CTA סטטי; פס התקדמות רק במאמרים (אם בכלל); reveals עדינים נשארים (transform/opacity,
  מכבדים reduced-motion); הסרת float/מרקיזה/מכונת כתיבה/פיצול מילים.
- **היקף:** shared (`Header`, `MotionEngine`, `motion.css`).

## ממצא 6 — כפילות תוכן ומבנה בית ↔ /solutions

- **דפוס:** מערך `SOLUTIONS` זהה (6 פריטים, אותם צ'קים, אותן תמונות, אותם SVG) מוגדר פעמיים —
  ב-`(site)/page.tsx` וב-`solutions/page.tsx`. הבית לא מקשר ל-/solutions בכלל.
- **החלפה:** מקור אחד `src/data/solutions.ts`; בבית — מקטע תמציתי שמקשר לעמוד המלא; ב-/solutions —
  שורות אדיטוריאליות מלאות.
- **היקף:** shared data + שני עמודים.

## ממצא 7 — Header/Footer

- **Header:** אין קישור ל-`/rooms` (!העמוד המסחרי המרכזי אינו נגיש מהניווט הראשי); "תקנון" תופס מקום
  בניווט ראשי; CTA מכונת-כתיבה.
- **Footer:** 4 אייקוני רשתות חברתיות מתים (בלי קישורים); "הצהרת נגישות" ו"מדיניות ביטולים" הם `<span>`
  מתים שנראים כקישורים; עמודת "פתרונות אירוח" — 4 תוויות שונות שכולן מצביעות על אותו `/solutions` עירום.
- **החלפה:** ניווט: ראשי · הדירות והסוויטות · פתרונות · מאמרים · שאלות נפוצות · אודות · צור קשר; CTA סטטי;
  הסרת אייקונים מתים וספאנים מתים; עמודת פתרונות עם עוגני `#sol-N` אמיתיים.
- **היקף:** shared בלבד.

## ממצא 8 — עוגני-בית במקום עמודים אמיתיים

- **דפוס:** CTA-ים מצביעים על `/#contact` ו-`/#apartments` במקום `/contact` ו-`/rooms` —
  ב-`/rooms`, `/about` (×2), `/contact`, `/solutions`, ובגוף המאמר הראשי.
- **החלפה:** קישורים קנוניים. (זה גם ממצא SEO — ראו SEO-AUDIT.)

## ממצא 9 — תוכן לא-ניתן-לאימות (חוצה עיצוב/תוכן)

בהתאם לתקדים הקיים בריפו (הוסרו: דירוג 4.8, חוות דעת מומצאות, ניוזלטר מזויף):

1. ציטוט "מתוך ספר האורחים" במאמר הדגל — social proof מיוחס ללא מקור. **מוסר.**
2. מרקיזת ארגונים (intel, רמב"ם, הטכניון, ZIM, אגד, מקורות, מת"ם) + המשפט המקביל ב-/about —
   name-dropping ללא אימות. **מוסר; יוחזר רק עם אישור בעלים.**
3. "14+ דירות וסוויטות" — סותר את הספירה החיה (8 מ-GuestHub). **מוחלף בעובדות הניתנות לאימות.**
4. תגי "פרטי הכרטיס מוצפנים"/"הנתונים שלכם מוגנים" ב-checkout — נשארים (טענה טכנית, לא social proof),
  אך מסומנים לבדיקת בעלים.

---

## כיוון עיצובי — "בית על קו המים" (אדיטוריאלי ים-תיכוני)

**עיקרון:** היוקרה מגיעה מצילום, פרופורציה, טיפוגרפיה וריסון — לא מאפקטים.

1. **טיפוגרפיה:** Frank Ruhl Libre (סריף עברי, כבר טעון בפרויקט כ-`--font-serif` מספר המותג) הופך לקול
   התצוגה — כותרות display/H1/H2 בסריף במשקל 500-600, גדול ורגוע. Open Sans נשאר לגוף ול-UI.
   זהו השדרוג בעל האימפקט הגבוה ביותר וללא תלות חדשה.
2. **צבע:** הפלטה הקיימת נשמרת (נייבי/אוקיינוס), אבל: מקטעים בהירים עוברים מלבן-כחלחל קר ל"נייר" חם
   (off-white חמים — "Mediterranean, warm" מהברייף), גרדיאנטים יורדים כמעט לגמרי, ו-`--color-sand`
   הקיים (זהב-חול) משמש כמבטא עדין. כהה = נייבי עמוק אחיד, לא גרדיאנט.
3. **משפחות עמודים:**
   - **בית** — נרטיב: hero צילומי full-bleed (וידאו הים הקיים) → משפט פתיחה אדיטוריאלי + עובדות חשופות →
     דירות (קרוסלה image-led) → פתרונות (רשימה אדיטוריאלית מקושרת ל-/solutions) → פס צילום full-bleed →
     "למה אנחנו" שקט → מאמרים → צור קשר.
   - **שיווק** (about/solutions/contact) — פתיחים צילומיים/טיפוגרפיים מגוונים, קומפוזיציות א-סימטריות.
   - **חדרים** — צילום שולט: פתיח שקט + גריד אדיטוריאלי עם דירה מובילה.
   - **מאמרים** — מגזין: רקע נייר, measure צר, סריף, standfirst; בלי רצועת נייבי.
   - **הזמנה** — משימתי, לבן, שקט; בלי hero גדול.
   - **משפטי** (terms/house-rules/faq) — מסמך שקט: פתיח טיפוגרפי קטן, שלד ה-toc הקיים נשמר.
   - **404** — שקט, ממותג, עם ניווט.
4. **גלים:** גל הפוטר נשאר בדיוק כפי שהוא (חוק פרויקט קבוע). גלי-הביניים מצטמצמים לנקודות מעבר
   בודדות ומוצדקות.
5. **תנועה:** כניסות עדינות בלבד (opacity/translate), reduced-motion נשמר, אפס גימיקים.

**מה לא משתנה:** לוגו, שם, פלטת הליבה, כל הלוגיקה העסקית, GuestHub, טפסים, ניתוב, גל הפוטר.
